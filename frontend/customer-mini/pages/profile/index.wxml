<view class="page">
  <view class="hero">
    <view class="name">{{memberName}}</view>
    <view class="desc">{{memberLevel}} · {{storeName}}</view>
  </view>
  <view wx:for="{{items}}" wx:key="*this" class="card">{{item}}</view>
</view>
